#pragma once
// 所有全局变量
int HighestScore = 0; // 用于记录最高得分
int len1, len2; // 用于改变输出时的空格
int Page = 1; // 用于确定当前是什么页面
char key; // 用于接收键盘输入的字符
int InputError = 0; // 用于表示是否输入错误
int aaa = 0; // 不加天天警告返回值没用到
int UserCount = 0; // 用户数量
int RankPage = 1; // 排名

int Speed = 200; // 用于记录当前速度，默认为200
// 用于记录用户的操作键
char up1 = 'w', down1 = 's', left1 = 'a', right1 = 'd';
// char up2 = 'i', down2 = 'k', left2 = 'j', right2 = 'l';

char pause1 = 'p', restart1 = 'r', exit1 = 'e'; // 暂停游戏键，重开游戏键，退出游戏键

int useArrowKeys=0; // 是否使用方向键
